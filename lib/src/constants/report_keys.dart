class ReportKeys {
  ReportKeys._();

  static const developerMode = "developerMode";

  /// Android only
  static const usbDebugging = "usbDebugging";

  /// Android & iOS
  static const debuggerAttached = "debuggerAttached";

  /// Android Emulator / iOS Simulator
  static const virtualDevice = "virtualDevice";

  /// Android Root / iOS Jailbreak
  static const deviceCompromised = "deviceCompromised";
}
